import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://azrael-landing-mlc8x85mg-twistedoliver211fs-1271.vercel.app',
  trailingSlash: 'ignore',
  build: {
    inlineStylesheets: 'auto',
  },
  compressHTML: true,
});
